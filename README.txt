Twitter Resource Adapter

This is basic JCA 1.6 resource adapter implementation for Twitter. See example MDB how it works.

Note that JBoss AS 7.0.0 doesn't support MDB. Also we have to set explicit dependency in MDB jar/META-INF/MANIFEST.MF - See example/pom.xml.
